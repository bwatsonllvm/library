name: Health Check - Docs Source Endpoints

run-name: Check docs source endpoint health (llvm.org/clang/lldb)

on:
  workflow_dispatch:
  schedule:
    - cron: "42 8 * * *"

permissions:
  contents: read

jobs:
  health:
    name: Validate docs source URLs and search endpoints
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Check docs sources health
        run: |
          python3 scripts/check-docs-sources-health.py \
            --catalog docs/sources.json \
            --timeout 20
